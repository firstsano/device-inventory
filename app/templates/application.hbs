<div class="inventory">
  <div class="container-fluid">
    <div class="row">
      <div class="col-xs-2"> {{nav-bar}} </div>
      <div class="col-xs-10">
        {{#each flashMessages.queue as |flash|}}
          {{flash-message flash=flash}}
        {{/each}}
        {{outlet}}
      </div>
    </div>
  </div>
</div>
